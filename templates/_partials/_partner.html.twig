{# ShowPartner include ds admin/showpartner and partner/partner #}

<div class="d-flex justify-content-center mt-5">
    <div class="card " style="width: 18rem;">
        <div class="card-body">
{{ dump(partner.isIsActive)}}

    {% if partner %}
            <h5 class="card-title">Fitness Drive - {{partner.namePartner}}  </h5>
            <p class="text-muted">Gérer par {{partner.user.username}} </p>
    {% endif %}
            <p class="card-text">
                <ul>
                    <li><a h ref="#">lien interne - Voir les permissions globales<a></li>
                    <li><a href="mailto:{{partner.user.email}} "><i class="bi bi-envelope-fill p-2"></i>{{partner.user.email}}<a></li>
                    <li><a href="tel:{{partner.phone}} "><i class="bi bi-telephone-fill p-2"></i>{{partner.phone}}<a></li>
                </ul>
                <!-- Rounded switch -->
                <div class="d-flex justify-content-around" >
                    <p>Active</p>
                    <label class="switch"> 
                        <input type="checkbox">
                        <span class="slider round"></span>
                    </label>
                </div>
            </p>
        </div>
        <div class="card-footer d-flex justify-content-center">
            <a type="button" class="btn-sm btn-warning" href="{{ path('partner_edit', {'id': partner.id}) }}">Modifier</a>
        </div>
    </div>
</div>