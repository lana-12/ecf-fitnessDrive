{# <showStructure include ds admin/showStructure #}

<div class="d-flex justify-content-center mt-5">    
    <div class="card" style="width: 18rem;">
        <div class="card-body">

        {% if structure %}
        
            <h5 class="card-title">Fitness Drive {{structure.partner.namePartner}} - {{structure.namestructure}} 
            </h5>
            <div class="d-flex justify-content-between">
                <p class="text-muted">Gérer par {{structure.user.username}} 
                </p>
        {% endif %}
            <!-- Structure Active ? -->
                <p>{{ structure.isActive ? 
                    '<span class="p-2">Active</span><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-circle-fill text-success" viewbox="0 0 16 16"><circle cx="8" cy="8" r="8"/></svg>' :'<span class="p-2">InActive</span><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-circle-fill text-danger" viewbox="0 0 16 16"><circle cx="8" cy="8" r="8"/></svg>'}}
                </p>
            </div>

            <p class="card-text">  
                <ul>
                    <li>
                        <a href="mailto:{{structure.user.email}} "><i class="fa fa-envelope p-2" aria-hidden="true"></i>{{structure.user.email}}<a>
                    </li>
                    <li>
                        <a href="tel:{{structure.phone}} "><i class="fa fa-phone p-2" aria-hidden="true"></i>{{structure.phone}}<a>
                    </li>
                </ul>
            </p>
        </div> 
        <div class="card-footer">
            <a href=" {{ path('structure_edit', {'id': structure.id})}} " type="button" class="btn-sm btn-warning">Modifier</a>
        </div>
    </div>
</div>