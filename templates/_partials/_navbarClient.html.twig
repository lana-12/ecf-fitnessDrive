<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><h1 class="titleCustom text-white">Fitness<span class="drive">Drive</span></h1></a>
    
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <li class="nav-item">
{# BACK PAGE HOME #} 
        {% if is_granted('ROLE_PARTNER') %}
            <a class="nav-link " href="{{ path( 'app_partner_show', {'id': partner.id}) }}">Accueil</a>
        {# {% endif %} #}
        
        {% elseif is_granted('ROLE_STRUCTURE') %}
            <a class="nav-link " href="{{ path( 'app_structure_show', {'id': structure.id}) }}">Accueil</a>
        {% endif %}
                </li>
                <li class="nav-item">
{# CHANGE ID + MDP #}
        {% if is_granted('ROLE_PARTNER') %}
            <a class="nav-link " href="{{ path( 'app_partner_edit', {'id': partner.id}) }}">Profil</a>
        {# {% endif %} #}
        
        {% elseif is_granted('ROLE_STRUCTURE') %}
            <a class="nav-link " href="{{ path( 'app_structure_edit', {'id': structure.id}) }}">Profil</a>
        {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                </li>
                <li class="nav-item">
                <a href="{{ path('app_logout') }}" class="nav-link">Déconnection</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

